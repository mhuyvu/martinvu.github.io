source "https://rubygems.org"

# Use GitHub Pages gem — includes Jekyll + supported plugins
gem "github-pages", group: :jekyll_plugins

# Windows compatibility (only used if on Windows)
gem "wdm", "~> 0.1.0" if Gem.win_platform?

# Extra plugins that GitHub Pages supports
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end
